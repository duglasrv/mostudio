# 2026-08-01 — Construcción inicial del portafolio RVstudio

- Se partió del template Mostudio (Colorlib) descomprimido de
  `mostudio-master.zip` y las 49 fotos originales en `Documents/fotos/`.
- Se revisaron visualmente las 49 fotos, se renombraron por categoría y se
  copiaron a `images/portfolio/`.
- Se llenaron todas las páginas con datos reales de Omar (bio, contacto,
  precios) y se tradujo el sitio completo al español.
- Iteraciones de precios: Q750/Q950/Q1,250 → Q900/Q1,150/Q1,500 (Instante /
  Momento / Legado) tras investigar mercado guatemalteco de fotografía.
- Se probó y luego se revirtió un sistema de "álbumes" con lightbox
  agrupado por evento en la galería — Omar prefirió el grid plano simple.
- Se eliminaron 8 fotos por decisión editorial de Omar; se agregaron 7 fotos
  nuevas (`nueva-01..07.jpg`, nombres genéricos a propósito).
- Se conectó `contact.html` a Formspree, reusando el mismo endpoint del
  portafolio de developer de Omar (decisión explícita suya).
- Se consolidó el proyecto de `mostudio-portfolio/mostudio-master/` (carpeta
  anidada) a `Documents/mostudio/` (una sola carpeta limpia).
- Primer commit y push a `https://github.com/duglasrv/mostudio` (rama `main`).
- Se armó la estructura SDD completa del proyecto (`CLAUDE.md`,
  `docs/architecture.md`, `docs/business-rules.md`, `docs/global-rules.md`,
  `docs/methodology.md`, esta carpeta `memory/`).
- **Pendiente**: deploy en Vercel + subdominio `foto.rvstudio.dev` (falta
  confirmar si el DNS de `rvstudio.dev` está en Namecheap o ya en Vercel).
- **Pendiente**: registrar este proyecto en Engram — el auto-detect falla
  por ambigüedad de cwd; debería resolverse solo si una futura sesión de
  Claude Code abre con working directory dentro de esta carpeta.
